import React from 'react';
import { SideNavigationProps, navbarTheme } from '../types';
export interface ThemeWrapperProps {
    navBackground: string;
    theme: navbarTheme;
}
export declare const SideNavigation: React.FC<SideNavigationProps>;
